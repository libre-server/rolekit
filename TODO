* rng

* roled suspend with save current states (for automatic wakeup, dbus activation)
  - status update

* async and simultaneous method call support for role handling
  - callback structuren (partly done)

* implement role handling
  - start (on review)
  - stop (on review)
  - restart (DONE)
  - deploy (package installation)
  - decommission (async still needed)
  - redeploy (outstanding)

* implement package handling
  - installPackages (outstanding)

* implement service handling
  - systemd targets (outstanding)
  - restartServices (on review)

* implement firewall handling (outstanding)
  - installFirewall
  - updateFirewall
  - uninstallFirewall

* handle role updates (outstanding, >F21)

* documentation (outstanding)
  - homepage
  - rolectl
  - roled
  - rolekit.dbus
  - rolekit.roles
    - role API
    - description
    - user parameters
  - rolekit

* translations (outstanding)
  - help text and man pages (?)

  - create and upload pot file (to zalada?)
  - adapt tx-pull target if needed
  - get translations
  - enable "%find_lang %{name} --all-name" and "%files -f %{name}.lang" in spec

DONE

* role instances DONE

* finalize role states

* rolectl
  - roled state
  - role handlling, state
  - instance handling, state
